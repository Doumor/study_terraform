# OpenROAD in terraform

This is a terraform script that should run openROAD, but it crashes on the verification build. I probably have something wrong with the openROAD versions.

Run:

```shell
terraform init
terraform apply
```
